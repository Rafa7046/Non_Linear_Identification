[package]
name = "rust_nlsi"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_nlsi"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
ndarray = "0.16"
numpy = "0.22"
rand = "0.8"
